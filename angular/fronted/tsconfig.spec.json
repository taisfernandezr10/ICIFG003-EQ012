{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "jasmine"
    ]
  },
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.ts", // Añadimos esto para que reconozca todos los archivos .ts
    "src/**/*.d.ts"
  ]
}